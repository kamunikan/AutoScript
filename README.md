```
apt update && apt upgrade -y --fix-missing && update-grub && sleep 2 && reboot
```
```
rm -rf Autoscripts.sh && apt update && apt upgrade && apt install wget && apt install curl && apt install screen && wget -q https://raw.githubusercontent.com/arismaramar/AutoScript/main/Autoscripts.sh && chmod +x Autoscripts.sh && screen -S apninstall ./Autoscripts.sh
```
